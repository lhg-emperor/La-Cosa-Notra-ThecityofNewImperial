using UnityEngine;

// Marker component: attach to the GameObject that should be used as Dialogue Panel
// when DialogueManager tries to auto-resolve UI references after a scene load.
public class CheckDialog : MonoBehaviour
{
    // Intentionally empty - marker only
}
